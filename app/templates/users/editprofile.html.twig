{% extends 'base.html.twig' %}

{% block title %}Modification du profil de
	{{ app.user.firstname }}
	{{ app.user.lastname }}
{% endblock %}

{% block body %}
	<div class="grix xs4 container">
		<div class="col-xs4">
			<h3>Modification du profil de
				{{ app.user.firstname }}
				{{ app.user.lastname }}</h3>
			{{ form(form) }}
			</div>

		</div>
	<button type="button" class="btn btn-outline-success m-4">
		<a class="nav-link" href="{{path('users_pass_modifier')}}">Edit password</a>
	</button>
	{% endblock %}
