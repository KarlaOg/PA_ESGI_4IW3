{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}


{% block title %} Validation de postuler  {% endblock %}


{% block body %}

    <h1> Validation pour postuler à l'offre</h1>


<div class="row">
		<div
			class="column large-12 tab-12">
			{{ form_start(form) }}
			{{ form_row(form) }}
			<button type="submit" class="btn btn-success">Sauvegarder</button>
			<a href="{{ path('offer_show', {'id': offer.id}) }}" class="btn btn-danger">Retour</a> 
			{{ form_end(form) }}
		</div>
	</div>

{% endblock %}